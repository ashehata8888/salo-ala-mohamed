# iOS Widget Setup Guide

Since modifying `.pbxproj` files directly via automation can corrupt your Xcode project, follow these manual steps to add the Widget to your iOS App:

1. Open Xcode by running `npx cap open ios` or double-clicking `ios/App/App.xcworkspace`.
2. In Xcode, go to **File** -> **New** -> **Target...**
3. Select **Widget Extension** and click Next.
4. Product Name: `WidgetExtension`
5. Make sure "Include Live Activity" and "Include Configuration App Intent" are **unchecked**. Click **Finish**.
6. If prompted to activate the scheme, click **Activate**.
7. Delete the autogenerated `WidgetExtension.swift` files inside the newly created group in Xcode.
8. Right-click the `WidgetExtension` folder in the Xcode navigator and click **Add Files to "WidgetExtension"...**
9. Select the `ios/App/WidgetExtension/WidgetExtension.swift` file we generated for you and make sure the "Target" selected is only `WidgetExtension`.
10. **Enable App Groups:**
    - Click your top-level project `App`.
    - Select the `App` target -> **Signing & Capabilities** -> click **+ Capability** -> select **App Groups**.
    - Add a new App Group: `group.com.salo.alahmoha`
    - Do the same for the `WidgetExtension` target, selecting the exact same App Group.
11. Build and Run on a Simulator or real device! You can now add the "Salah Reminder" widget to your iOS home screen.
